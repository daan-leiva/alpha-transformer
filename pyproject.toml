[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "alpha_transformer"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
where = ["alpha_transformer"]